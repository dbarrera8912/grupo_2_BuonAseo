<!DOCTYPE html>
<html lang="en">
  <%- include('../partials/head', {title: 'Oferta'}) %>
  <body>
    <%- include('../partials/header') %>
    <main class="ofertas_main">
        <article class="ofertas_main_section_article_titulo">
            <h1 class="ofertas_main_titulo">Productos en oferta</h1>
          </article>
      <section class="ofertas_main_section_productos_destacados">
        <article class="ofertas_main_section_article_imagenes">
          <% productosEnOferta1.forEach(({id,name,price,image,discount}) => { %>
            <div class="home_main_section_productos_destacados_caja">
              <a href="/products/detail/<%= id %>"><img class="home_main_section_productos_destacados_img--mobile"
                src="<%= image %>"
                alt="imagen producto"
            /></a>
              <div class="home_main_section_productos_destacados_parrafo--mobile">
                <p class="home_main_parrafo_mobile_titulo"><%= name%></p>
                <br>
                <p class="home_main_parrafo_mobile_precio">
                  <% if ( discount > 0 ) { %>
                    <% let precioDescuento = price - (price * (discount / 100)) %> 
                    <span class="precioDesc">$<%= precioDescuento %> </span>
                    <s>$<%= price %></s>
                  <% } else { %>
                    <span class="precioDesc">$<%= price %></span>
                  <% } %>
                </p>
              </div>
            </div>
          <% }) %>
        </article>
      </section>
        <section class="ofertas_main_section_productos_destacados">
          <article class="ofertas_main_section_article_imagenes">
            <% productosEnOferta2.forEach(({id,name,price,image,discount}) => { %>
              <div class="home_main_section_productos_destacados_caja">
                <a href="/products/detail/<%= id %>"><img class="home_main_section_productos_destacados_img--mobile"
                  src="<%= image %>"
                  alt="imagen producto"
              /></a>
                <div class="home_main_section_productos_destacados_parrafo--mobile">
                  <p class="home_main_parrafo_mobile_titulo"><%= name%></p>
                  <br>
                  <p class="home_main_parrafo_mobile_precio">
                    <% if ( discount > 0 ) { %>
                      <% let precioDescuento = price - (price * (discount / 100)) %> 
                      <span class="precioDesc">$<%= precioDescuento %> </span>
                      <s>$<%= price %></s>
                    <% } else { %>
                      <span class="precioDesc">$<%= price %></span>
                    <% } %>
                  </p>
                </div>
              </div>
            <% }) %>
          </article>
        </section>
          <section class="ofertas_main_section_productos_destacados">
            <article class="ofertas_main_section_article_imagenes">
              <% productosEnOferta3.forEach(({id,name,price,image,discount}) => { %>
                <div class="home_main_section_productos_destacados_caja">
                  <a href="/products/detail/<%= id %>"><img class="home_main_section_productos_destacados_img--mobile"
                    src="<%= image %>"
                    alt="imagen producto"
                /></a>
                  <div class="home_main_section_productos_destacados_parrafo--mobile">
                    <p class="home_main_parrafo_mobile_titulo"><%= name%></p>
                    <br>
                    <p class="home_main_parrafo_mobile_precio">
                      <% if ( discount > 0 ) { %>
                        <% let precioDescuento = price - (price * (discount / 100)) %> 
                        <span class="precioDesc">$<%= precioDescuento %> </span>
                        <s>$<%= price %></s>
                      <% } else { %>
                        <span class="precioDesc">$<%= price %></span>
                      <% } %>
                    </p>
                  </div>
                </div>
              <% }) %>
            </article>
          </section>
          
    </main>
    <%- include('../partials/footer') %>
    </body>
</html>
